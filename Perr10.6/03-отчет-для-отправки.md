# Задание 10.6 — отчёт для отправки

Текст ниже можно копировать в форму сдачи задания на university.zerocoder.ru.
Ссылки проверены и открываются.

---

## Что сделано

Автопостинг из задания 10.5 доведён до полной версии: цепочка n8n сама пишет текст
поста, придумывает задание художнику, рисует картинку (`gpt-image-1-mini`) и
публикует пост с картинкой в Telegram (`sendPhoto`, подпись — текст поста). Вебхук
закрыт токеном (Header Auth): без заголовка `Authorization` n8n отвечает 403 и
даже не запускает цепочку, с правильным токеном — 200 и публикация.

Проверено снаружи, с постороннего сервера (VPS в Германии), через боевой адрес
**https://sintaition.sintaris.net**:

| Запрос | Ответ | Время |
|---|---|---|
| без заголовка `Authorization` | **403** | 0,2 с |
| с заголовком `Authorization: Bearer …` | **200**, пост с картинкой | 16,1 с |

Полный протокол обоих запусков — в [`results/проверка-защиты.txt`](results/проверка-защиты.txt).

## Проверка двумя запросами

```bash
# без токена — должно вернуть 403
curl -i -X POST https://sintaition.sintaris.net/webhook/zerocoder-autopost-image \
  -H "Content-Type: application/json" \
  -d '{"topic": "Тема поста"}'

# с токеном — должно вернуть 200 и ссылку на пост с картинкой
curl -X POST https://sintaition.sintaris.net/webhook/zerocoder-autopost-image \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <токен>" \
  -d '{"topic": "Тема поста"}'
```

## Опубликованный пост (можно открыть сразу)

- <https://t.me/business_ai_automate/7> — текст + картинка, полный сквозной тест снаружи

## Сверка с требованиями задания «Что нужно сдать»

| # | Требование | Статус | Файл / ссылка |
|---|---|---|---|
| 1 | Скриншот полной схемы workflow в n8n | ⬜ нужно снять руками | см. раздел «Какие скриншоты нужны» ниже |
| 2 | Скриншот успешного Execution | ⬜ нужно снять руками | — |
| 3 | Скриншот итогового поста в Telegram (текст + изображение) | ⬜ нужно снять руками, либо приложить прямую ссылку | <https://t.me/business_ai_automate/7> |
| 4 | Скриншот ответа со статусом 200 | ⬜ нужно снять руками (текст уже есть в проверка-защиты.txt) | [`results/проверка-защиты.txt`](results/проверка-защиты.txt) |
| 5 | Скриншот ошибки 403 без токена | ⬜ нужно снять руками (текст уже есть в проверка-защиты.txt) | [`results/проверка-защиты.txt`](results/проверка-защиты.txt) |
| 6 | JSON-файл экспортированного workflow | ✅ готово | [`workflow.json`](workflow.json) |

## Критерии зачёта

| Критерий | Выполнено |
|---|---|
| Пост публикуется автоматически | ✅ |
| Генерируется и текст, и изображение | ✅ |
| Настроена защита через Authorization header | ✅ |
| Система работает с удалённого сервера | ✅ |

## Какие скриншоты нужны (5 штук)

Кладите в `Perr10.6/results/screenshots/` — как и в задании 10.5.

| # | Файл (предлагаемое имя) | Что именно снять | Где открыть |
|---|---|---|---|
| 1 | `0-n8n-workflow.png` | Полную схему цепочки целиком (все 13 узлов, от Webhook до Respond to Webhook) | <https://sintaition.sintaris.net> → Workflows → «Zerocoder 10.6 — Автопостинг с картинкой» |
| 2 | `1-n8n-execution-success.png` | Execution со статусом Success — тот же запуск, что дал пост №7 | там же → вкладка Executions → выбрать успешный запуск |
| 3 | `2-telegram-post-with-image.png` | Сам пост в канале: видно и картинку, и текст-подпись | Telegram, канал `@business_ai_automate`, пост <https://t.me/business_ai_automate/7> |
| 4 | `3-response-200.png` | Терминал с ответом 200 и JSON (`ok: true`, `with_image: true`, ссылка на пост) | запустить `python main.py "тема"` и снять вывод, либо снять готовый вывод из `results/проверка-защиты.txt` |
| 5 | `4-response-403.png` | Терминал с ответом 403 без токена | запустить `python main.py --no-token "тема"` и снять вывод, либо снять готовый вывод из `results/проверка-защиты.txt` |

Пункты 4 и 5 можно не гонять заново — весь нужный текст уже сохранён в
`results/проверка-защиты.txt`; достаточно открыть этот файл и снять с него
два скриншота (раздел 1 — для 403, раздел 2 — для 200).

## Файлы к отчёту

- `workflow.json` — экспортированная цепочка, 13 узлов (пункт 6 задания).
- `main.py` — скрипт с POST-запросом, флаг `--no-token` для проверки защиты.
- `результаты.md` — подробное описание того, что и как сделано.
- `demo.md` — как показать работу и куда смотреть проверяющему.
- `results/проверка-защиты.txt` — текстовый протокол обоих запусков (403 и 200), сделанных снаружи.

## Доступ проверяющему (если нужен доступ в редактор n8n)

Токен для заголовка `Authorization` и пароль от редактора — на сервере, в
`~/n8n-new/.env`. Подробности — см. [`demo.md`](demo.md), раздел «Доступ».
